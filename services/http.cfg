# timeout in milliseconds for the http requests (optional, defaults to 5000)
timeout=5000

# the interval in milliseconds when to find new refresh candidates
# (optional, defaults to 1000)
granularity=1000

# whether to substitute the current time or state value into the URL
# (optional, defaults to true)
#format=

# configuration of the first cache item
#<id1>.url=
#<id1>.updateInterval=

# configuration of the second cache item  
#<id2>.url=
#<id2>.updateInterval=

tvheadend.url=http://IP-TVHEADEND:9981/api/dvr/entry/grid_upcoming?sort=start
tvheadend.updateInterval=300000

tvhSubscriptions.url=http://IP-TVHEADEND:9981/api/status/subscriptions
tvhSubscriptions.updateInterval=100000
